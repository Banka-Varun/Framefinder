 'use client';
import {useState} from 'react';
import Link from './app-link';
export default function AdminSetup({user}:{user:{id:string;username:string}}){const[message,setMessage]=useState('');async function copy(){try{await navigator.clipboard.writeText(user.id);setMessage('Account ID copied. Paste it into the Vercel Value field.');}catch{setMessage('Select the account ID above and copy it manually.');}}return <section className="section"><p className="eyebrow">FOR THE SITE OWNER</p><h1>Set up administrator access.</h1><div className="panel"><p>You are signed in as <strong>@{user.username}</strong>. This account does not currently have administrator access.</p><p>Use the exact account ID below as the value of the Vercel environment variable.</p><label>Variable name<input readOnly value="ADMIN_USER_IDS"/></label><label>Value — your account ID<input readOnly value={user.id} onFocus={e=>e.target.select()}/></label><button className="button primary" onClick={copy}>Copy admin value</button>{message&&<p role="status">{message}</p>}<ol><li>Open your Framefinder project in Vercel.</li><li>Go to Settings → Environment Variables → Add.</li><li>Enter <code>ADMIN_USER_IDS</code> as the name and paste the copied account ID as the value. Enable Production and save.</li><li>Redeploy the latest version from Deployments.</li><li>Refresh Framefinder and return to this page. Sign in with this same account.</li></ol><p>Your username and email are different from this account ID. There is no extra admin password. Only someone with access to the Vercel project can grant this role.</p><Link href="/settings">Return to account settings</Link></div></section>;}
