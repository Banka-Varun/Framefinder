'use client';
import {useEffect,useState,useRef,type FormEvent} from 'react';
import Link from './app-link';
import {MessageCircle,Send,ArrowLeft} from 'lucide-react';
import {api,useLoad} from './client-data';
export default function TicketChat({conversationId}:{conversationId?:string}){const chatEnd=useRef<HTMLDivElement>(null);const[older,setOlder]=useState<any[]>([]),[hasOlder,setHasOlder]=useState(true);const[revision,refresh]=useState(0),[text,setText]=useState(''),[amount,setAmount]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const{data,error:loadError}=useLoad('conversations'+(conversationId?'/'+conversationId:''),revision);useEffect(()=>{chatEnd.current?.scrollIntoView({block:'nearest'});},[data?.messages?.length]);useEffect(()=>{const tick=setInterval(()=>{if(!document.hidden)refresh(x=>x+1)},7000);return()=>clearInterval(tick)},[conversationId]);async function send(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await api('conversations/'+conversationId+'/messages',{message:text,nonce:crypto.randomUUID()});setText('');refresh(x=>x+1)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}async function offer(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await api('conversations/'+conversationId+'/offers',{amount:Math.round(Number(amount)*100)});setAmount('');refresh(x=>x+1)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}async function decide(id:string,action:string){setBusy(true);try{await api('conversations/'+conversationId+'/offers/'+id,{action});refresh(x=>x+1)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
if(!conversationId)return <section className="section"><p className="eyebrow">TALK ABOUT YOUR TICKETS</p><h1>Conversations.</h1>{loadError&&<p className="notice error">{loadError}</p>}<div className="conversation-list">{data?.conversations.map((c:any)=><Link href={'/messages/'+c.id} key={c.id} className="conversation-row"><MessageCircle/><div><h3>{c.movie}</h3><p>@{data.viewerId===c.buyer_id?c.seller:c.buyer} · {c.ticket_status.replaceAll('_',' ')}</p></div><span>Open chat →</span></Link>)}</div>{data&&!data.conversations.length&&<div className="empty"><MessageCircle size={35}/><h2>No conversations yet.</h2><p>Open a public ticket listing and choose “Chat & negotiate”. Sellers see incoming conversations here.</p><Link href="/tickets" className="button primary">Find tickets</Link></div>}</section>;
if(!data)return <div className="empty">{loadError||'Opening conversation…'}</div>;const c=data.conversation,active=['pending_verification','verified'].includes(c.ticket_status)&&c.show_at>new Date().toISOString();return <section className="section"><Link href="/messages" className="back-link">← All conversations</Link><div className="section-heading"><div><p className="eyebrow">PRIVATE BUYER–SELLER CHAT</p><h1>{c.movie}</h1><p>Talking with @{data.viewerId===c.buyer_id?c.seller:c.buyer}</p></div><Link className="button secondary" href={'/tickets/'+c.ticket_id}>View listing</Link></div>{(error||loadError)&&<p className="notice error" role="alert">{error||loadError}</p>}<div className="chat-layout"><div className="panel"><div className="chat-messages" aria-live="polite">{data.hasOlder&&hasOlder&&<button className="text-button" onClick={async()=>{try{const before=older[0]?.created_at||data.messages[0]?.created_at;const result=await api('conversations/'+conversationId+'?before='+encodeURIComponent(before));setOlder([...result.messages,...older]);setHasOlder(result.hasOlder)}catch(e){setError((e as Error).message)}}}>Load earlier messages</button>}{data.messages.length?[...older,...data.messages].filter((m,i,rows)=>rows.findIndex(x=>x.id===m.id)===i).map((m:any)=><div key={m.id} className={'chat-bubble '+(m.sender_id===data.viewerId?'mine':'')}><p>{m.message}</p><small>{new Date(m.created_at).toLocaleString('en-IN',{dateStyle:'short',timeStyle:'short'})}</small></div>):<p className="muted">Ask about the show or discuss a price. Don’t share passwords, OTPs or payment credentials.</p>}<div ref={chatEnd}/></div><form onSubmit={send} className="chat-compose"><textarea aria-label="Message" value={text} onChange={e=>setText(e.target.value)} maxLength={2000} rows={2} placeholder="Write a message…" required disabled={!active}/><button className="button primary" disabled={busy||!text.trim()||!active}><Send size={17}/>Send</button></form>{!active&&<p className="small">This listing is closed. The conversation is read-only.</p>}</div><aside className="panel"><h2>Price discussion</h2><p className="small">Original total: ₹{c.face_value/100}</p><p>Asking total: <strong>₹{c.price/100}</strong></p><form onSubmit={offer}><label>Your offer · total ₹<input type="number" min="0.01" max="100000" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)} required disabled={!active}/></label><button className="button secondary" disabled={busy||!amount||!active}>Send offer / counteroffer</button></form><p className="small">An accepted offer records the agreed total. It does not verify, reserve or transfer the tickets.</p>{data.offers.map((o:any)=><article className="offer-card" key={o.id}><strong>₹{o.amount/100}</strong><span className="tag">{o.status}</span><p className="small">{o.sender_id===data.viewerId?'You offered':'Other participant offered'}</p>{o.status==='pending'&&active&&<div className="actions">{o.sender_id!==data.viewerId?<><button className="button primary" disabled={busy} onClick={()=>decide(o.id,'accept')}>Accept</button><button className="text-button" disabled={busy} onClick={()=>decide(o.id,'decline')}>Decline</button></>:<button className="text-button" disabled={busy} onClick={()=>decide(o.id,'withdraw')}>Withdraw</button>}</div>}</article>)}</aside></div></section>}
