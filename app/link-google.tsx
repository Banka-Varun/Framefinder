'use client';
import {useState,type FormEvent} from 'react';
import {api} from './client-data';
import Link from './app-link';
export default function LinkGoogle(){const[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');try{const result=await api('auth/link-google',{password:new FormData(e.currentTarget).get('password')});location.assign(result.next);}catch(e){setError((e as Error).message);setBusy(false);}}return <section className="section"><p className="eyebrow">ONE LAST STEP</p><h1>Connect Google to your account.</h1><form className="panel" onSubmit={submit}><p>Google verified your email. A Framefinder account already uses it. Enter your existing Framefinder password once to connect them and keep your profile, followers and watch history.</p><label>Framefinder password<input type="password" name="password" autoComplete="current-password" required maxLength={128}/></label><button className="button primary" disabled={busy}>{busy?'Connecting…':'Connect Google and sign in'}</button>{error&&<p className="notice error" role="alert">{error}</p>}<p>Next time, Continue with Google signs you straight in.</p><p><Link href="/forgot-password">Forgot your Framefinder password?</Link></p><Link href="/login">Back to sign in</Link></form></section>;}
